<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base XML theme; the real theming happens in Compose (LlamaChatTheme).
         This only needs to supply a window background and hide the action bar
         so there is no flash before Compose draws. -->
    <style name="Theme.LlamaChat" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/dc_page</item>
        <item name="android:statusBarColor">@color/dc_primary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@android:color/white</item>
    </style>
</resources>
